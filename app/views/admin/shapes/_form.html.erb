<%= bootstrap_form_with(model: [:admin, shape]) do |form| %>
  <%= form.alert_message "Please correct the #{pluralize(form.object.errors.count, 'error')} below." %>
  <%= form.text_field :name %>
  <%= form.check_box :customize %>
  <%= form.primary %>
<%- end -%>
